
export interface NavItem {
  label: string;
  path: string;
}

export interface Review {
  name: string;
  text: string;
  rating: number;
  date: string;
}

export interface GalleryImage {
  url: string;
  title: string;
}
